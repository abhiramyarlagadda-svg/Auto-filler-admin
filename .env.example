# DEAD FILE — Supabase env vars are no longer used.
# The admin panel collects a GitHub Personal Access Token at runtime
# (stored in browser localStorage). No build-time secrets are required.
#
# Safe to delete:
#   rm Auto-filler-admin-main/.env.example
