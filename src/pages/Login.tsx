// DEAD CODE — kept only because Claude's auto-mode blocked the rm.
// Safe to delete:
//   rm Auto-filler-admin-main/src/pages/Login.tsx
//
// Supabase email+password auth was removed. The admin panel now uses a
// GitHub Personal Access Token collected at runtime by ./PatSetup.tsx.
export default function Login() { return null; }
